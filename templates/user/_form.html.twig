{{ form_start(form) }}
<div class="card-body">
    {{ form_widget(form,{'attr': {'novalidate': 'novalidate'}}) }}
</div>
<div class="card-footer">
    <button type="submit" class="{{ button_css|default("btn btn-dark btn-sm") }}">
        <i class="fa fa-save" aria-hidden="true"></i> {{ button_label|default('label.update'|trans) }}
    </button>
</div>
{{ form_end(form) }}


